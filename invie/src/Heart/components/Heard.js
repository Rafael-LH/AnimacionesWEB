import React from 'react'

// Dumb component, Componente tonto, UI component, vista del MVC
const RenderHeart = props => (
        <div className='like is-liked' style={props.style}>
        </div>
)

export default RenderHeart