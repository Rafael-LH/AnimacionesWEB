import React, {Component} from 'react'

// UI Component, Component de UI, Dumb Component, Componente Tonto, vista del MVC
const Home = props =>(
            <h1>{props.saludo}</h1>
)
export default Home